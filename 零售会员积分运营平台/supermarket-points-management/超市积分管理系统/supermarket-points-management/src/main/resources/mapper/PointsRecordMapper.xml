<?xml version="1.0" encoding="UTF-8"?>
<!DOCTYPE mapper PUBLIC "-//mybatis.org//DTD Mapper 3.0//EN" "http://mybatis.org/dtd/mybatis-3-mapper.dtd">
<mapper namespace="com.supermarket.points.mapper.PointsRecordMapper">
    <insert id="insert" parameterType="com.supermarket.points.model.entity.PointsRecord">
        INSERT INTO points_record (user_id, commodity_id, points, type, create_time)
        VALUES (#{userId}, #{commodityId}, #{points}, #{type}, #{createTime})
    </insert>

    <select id="selectByUserId" resultType="com.supermarket.points.model.entity.PointsRecord">
        SELECT * FROM points_record WHERE user_id = #{userId}
        ORDER BY create_time DESC
            LIMIT #{pageNum}, #{pageSize}
    </select>

    <select id="selectTotalByUserId" resultType="java.lang.Integer">
        SELECT COUNT(*) FROM points_record WHERE user_id = #{userId}
    </select>
</mapper>